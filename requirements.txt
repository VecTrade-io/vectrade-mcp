# VecTrade MCP Integration Kit — development/test dependencies
#
# The setup wizard and verification scripts use only the standard library.
# Install these for local development and testing:

pytest>=8.0
pytest-cov>=5.0
ruff>=0.4
